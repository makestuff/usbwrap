A thin wrapper around libusb.

The project build assumes a directory structure like this:

src
|
+-apps
|
+-libs
| |
| +-usbwrap
|
+-3rd

*** BUILDING ON WINDOWS ***

make -f Makefile.win32

*** BUILDING ON LINUX ***

make -f Makefile.linux
